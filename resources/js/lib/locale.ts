/**
 * Application locale used for all date/time formatting.
 *
 * A fixed locale ensures SSR and client render identical text,
 * avoiding hydration mismatches. Matches the app's target audience.
 */
export const APP_LOCALE = 'sv-SE';
